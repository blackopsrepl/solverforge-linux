# SolverForge — Restic backup configuration (default layer)
# These are safe defaults. Set your actual values in:
#   ~/.config/solverforge/backup.conf

RESTIC_REPO=""
RESTIC_PASSWORD_FILE=""
RESTIC_EXCLUDE_FILE=""

BACKUP_PATHS=()
